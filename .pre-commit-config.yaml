repos:

  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.14.8 # Ruff version.
    hooks:
      - id: ruff-check
        args: [ --fix ]
        types_or: [ python, pyi ]
      # Run the formatter.
      - id: ruff-format
        types_or: [ python, pyi ]
